@model RulesViewModel
@{
    ViewData["Title"] = "Rules Analyzer";
    ViewData["Instance"] = Model.InstanceName;
}

@section AddToHead{
    <link rel="stylesheet" type="text/css" href="@Url.Content("~/Static/Styles/Views/rules.css")">
    <script type="text/javascript" src="@Url.Content("~/Static/Scripts/Views/rules.js")"></script>
}

<div>
    <div class="sub-header">Signal Rules: @Model.SignalRules.Count</div>
    <table class="table table-hover table-striped rules-table" id="signalRulesTable" style="width: 100%">
        <thead>
            <tr>
                <th>Signal Rule</th>
                <th>Profit</th>
                <th>Fees</th>
                <th>Cost</th>
                <th>Trades</th>
                <th>Orders</th>
                <th>Swaps</th>
                <th>Avg. Age</th>
                <th>Avg. Margin</th>
                <th>Avg. Margin DCA</th>
                <th>Avg. DCA</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var rule in Model.SignalRules)
            {
            <tr>
                <td>@rule.Key</td>
                <td>@rule.Value.TotalProfit.ToString("0.00000000")</td>
                <td>@rule.Value.TotalFees.ToString("0.00000000")</td>
                <td>@rule.Value.TotalCost.ToString("0.00000000")</td>
                <td>@rule.Value.TotalTrades</td>
                <td>@rule.Value.TotalOrders</td>
                <td>@rule.Value.TotalSwaps</td>
                <td>@rule.Value.Age.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.Margin.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.MarginDCA.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.DCA.DefaultIfEmpty(0).Average().ToString("0.00")</td>
            </tr>
            }
        </tbody>
    </table>

    <div class="sub-header">Trading Rules: @Model.TradingRules.Count</div>
    <table class="table table-hover table-striped rules-table" id="tradingRulesTable" style="width: 100%">
        <thead>
            <tr>
                <th>Trading Rule</th>
                <th>Profit</th>
                <th>Fees</th>
                <th>Cost</th>
                <th>Trades</th>
                <th>Orders</th>
                <th>Swaps</th>
                <th>Avg. Age</th>
                <th>Avg. Margin</th>
                <th>Avg. Margin DCA</th>
                <th>Avg. DCA</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var rule in Model.TradingRules)
            {
            <tr>
                <td>@rule.Key</td>
                <td>@rule.Value.TotalProfit.ToString("0.00000000")</td>
                <td>@rule.Value.TotalFees.ToString("0.00000000")</td>
                <td>@rule.Value.TotalCost.ToString("0.00000000")</td>
                <td>@rule.Value.TotalTrades</td>
                <td>@rule.Value.TotalOrders</td>
                <td>@rule.Value.TotalSwaps</td>
                <td>@rule.Value.Age.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.Margin.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.MarginDCA.DefaultIfEmpty(0).Average().ToString("0.00")</td>
                <td>@rule.Value.DCA.DefaultIfEmpty(0).Average().ToString("0.00")</td>
            </tr>
            }
        </tbody>
    </table>
</div>
