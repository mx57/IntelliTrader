## 2026-08-09 - Динамическое переключение светлой/темной тем и предотвращение мерцания (FOUC)
**Инсайт:** При реализации переключения тем оформления в веб-интерфейсе критически важно: 1) избегать мерцания интерфейса (FOUC), внедряя сверхлегкий инлайновый скрипт в `<head>`, навешивающий класс `light-theme` на элемент `<html>` до рендеринга `<body>`; 2) использовать централизованные CSS-переменные для стилизации DataTables, Bootstrap-карточек и выпадающих меню; 3) оповещать другие интерактивные компоненты (например, экземпляры Chart.js) через кастомные глобальные события `themeChanged` для мгновенной перерисовки шкал и сеток без перезагрузки страницы.
**Действие:** Объявлять CSS-переменные на `:root` и `html.light-theme`, синхронизировать интерфейсы через кастомные события Window и обновлять динамические инстансы Chart.js через вызов метода `.update()`.

## 2026-08-08 - Высококачественные интерактивные HTML-отчеты бэктестинга и расчет просадки по эквити
**Инсайт:** При генерации насыщенных, динамических HTML-отчетов в C# использование интерполированных строк (`$""`) приводит к синтаксическому хаосу и конфликтам компилятора (CS1012, CS1525) с фигурными скобками CSS, JS-функциями и ES6 template literals (`${}`). Использование raw string literals C# 11 (`"""..."""`) в сочетании с явной заменой плейсхолдеров (`.Replace`) полностью устраняет любые конфликты разметки и кода. Кроме того, при расчете максимальной просадки (Maximum Drawdown) в симуляциях торговли оценка на базе только денежного баланса (cash balance) дает критически неверные завышенные результаты (~99.9%), так как свободный кэш падает до нуля при активном наборе DCA позиций. Корректный расчет просадки должен выполняться исключительно по общему эквити портфеля (Equity = Cash + Value of Open Positions) на каждом шаге симуляции.
**Действие:** Формировать HTML-шаблоны через чистые raw string literals C# 11 с плейсхолдерами вида `{{PLACEHOLDER}}` и вызывать метод `.Replace()`. Рассчитывать Drawdown и другие финансовые метрики на основе полной оценки портфеля (Equity Curve), а не только на свободном остатке баланса.

## 2026-08-07 - Высокопроизводительный бенчмаркинг бэктестинга и замена ZeroFormatter
**Инсайт:** Библиотека ZeroFormatter содержит устаревшую логику генерации кода через Reflection.Emit, несовместимую с современными .NET Core средами (начиная с .NET Core 3.0, включая .NET 8.0/10.0), из-за чего выбрасывает исключение BadImageFormatException. При создании кроссплатформенных утилит бенчмаркинга на современных платформах использование ручной сериализации на базе BinaryWriter/BinaryReader обеспечивает идеальную стабильность, полное отсутствие аллокаций и колоссальный прирост производительности. Также при размещении исполняемых проектов утилит внутри папок библиотек необходимо явно отключать рекурсивную сборку в .csproj родителя через `<Compile Remove="..." />` во избежание конфликтов языковых стандартов C#.
**Действие:** Разрабатывать новые модули сериализации снимков рынка на базе чистого BinaryWriter/BinaryReader для максимальной переносимости и скорости. Защищать компиляцию родительских проектов с помощью тегов удаления файлов.

## 2026-08-06 - Динамическая визуализация уровней DCA на веб-панели управления (Dashboard)
**Инсайт:** При реализации визуализации сложных торговых состояний (таких как расчет триггерной цены DCA с учетом уже затраченной стоимости, комиссий и дополнительных расходов) критически важно переносить математические расчеты на сторону сервера (в C#-контроллер), возвращая на клиент детерминированную структуру данных `DcaLevels`. Это исключает дублирование логики и расхождение в расчетах на фронтенде, а также значительно облегчает рендеринг динамических таблиц на стороне клиента.
**Действие:** Производить расчет ожидаемых/активных уровней DCA на сервере, используя `tradingPair.Cost`, `tradingPair.Fees` и настройки `tradingService.Config.DCALevels`, возвращая структурированный массив. На фронтенде выполнять только легкий декларативный рендеринг HTML-таблицы с цветовыми акцентами для разных статусов (Completed, Next, Upcoming).

## 2026-08-02 - Динамическое расширение шагов DCA на основе волатильности
**Инсайт:** Во избежание преждевременного срабатывания усредняющих ордеров (DCA) на резких проливах рынка необходимо динамически расширять шаг DCA (делать целевую маржу NextDCAMargin более широкой/отрицательной) на основе рыночной волатильности. В качестве быстрых метрик волатильности отлично подходят текущий спред (CurrentSpread) и историческая волатильность по сигналам (Volatility).
**Действие:** При расчете условий DCA в DcaProcessor масштабировать NextDCAMargin с помощью динамического коэффициента, вычисляемого как отношение текущего спреда/волатильности к базовым значениям (0.2% для спреда и 4.0 для исторической волатильности), с ограничением максимального расширения (до 5.0x).

## 2026-08-01 - Интеграция реального времени и Web Audio API в Web-интерфейсе
**Инсайт:** Автовоспроизведение звуков в современных браузерах блокируется до совершения первого пользовательского действия (клик/интеракция). Для реализации надежного звукового оповещения о сделках необходимо: 1) Динамически генерировать звук через Web Audio API, избавляя проект от внешних аудио-файлов, снижая нагрузку и убирая риск 404 ошибок; 2) Добавлять элементы интеракции (например, кнопку проверки звука "Test Audio") для инициализации и возобновления работы AudioContext со стороны пользователя; 3) Давать возможность отключения звуков в localStorage.
**Действие:** При интеграции аудио-оповещений в Razor-представления использовать чистый ES6 JS с динамическим синтезированием аудио-частот через `OscillatorNode` и `GainNode` на событии клика для обхода блокировок воспроизведения.

## 2026-07-29 - Устранение коллизий ID элементов терминалов логов и интеграция Chart.js
**Инсайт:** Удаление элементов UI, которые кажутся устаревшими или избыточными, может вызвать критические ошибки JavaScript на клиенте, если глобальные скрипты (например, `dashboard.js`) жестко привязаны к их селекторам. При проектировании расширений интерфейса (например, добавление графиков Chart.js) важно бесконфликтно сосуществовать с исходными элементами, разделяя их ID (например, `logTerminal` vs `tradeLogTerminal`) и обновляя соответствующие селекторы в скриптах.
**Действие:** Всегда проверять зависимости во внешних JS-файлах перед изменением или удалением разметки в `.cshtml`. Использовать уникальные пространства имен ID для новых интерактивных компонентов.

## 2025-07-15 - Live high-performance seek-based backward log polling
**Инсайт:** Считывание логов для веб-интерфейса в реальном времени требует высокой скорости и минимальной нагрузки на процессор и память. Использование полного чтения файлов (таких как ReadAllLines) на больших файлах логов приводит к OutOfMemoryException и деградации производительности. При этом асинхронные скрипты в Razor-представлениях должны использовать чистый ES6 JavaScript/fetch API, привязанный к событию DOMContentLoaded, чтобы предотвратить гонки загрузки библиотек (например, jQuery).
**Действие:** Использовать оптимизированный алгоритм обратного чтения файлов (seek-based backward stream reader) с FileShare.ReadWrite для бесконфликтного чтения активных лог-файлов. Встраивать скрипты опроса в Razor-представления только через vanilla ES6 JS, привязанный к DOMContentLoaded.

## 2025-06-30 - Рефакторинг инициализации ExchangeService и оптимизация Stats

**Инсайт:** Инициализация коллекций `Tickers` и `markets` внутри метода `Start` (после асинхронного вызова API) создавала риск `NullReferenceException` для других сервисов, обращающихся к бирже в момент запуска. Кроме того, алгоритм накопления баланса в `Stats` требовал строгого соблюдения линейной сложности $O(N)$ для предотвращения зависаний на больших объемах логов сделок.

**Действие:** Всегда инициализировать основные коллекции-члены (`ConcurrentDictionary`, `ConcurrentBag`) в конструкторе сервиса. Для расчета временных рядов доходности использовать один проход по отсортированным данным, аккумулируя результат в итоговый словарь.

## 2025-07-02 - Оптимизация парсинга логов сделок (GetTrades)

**Инсайт:** Метод `HomeController.GetTrades` считывал и парсил ВСЕ файлы `*-trades.txt` при каждом запросе к Dashboard, Stats и Rules. Это создавало линейную деградацию производительности по мере накопления истории торгов.

**Действие:** Использовать кэширование результатов парсинга в `static ConcurrentDictionary`, проверяя `LastWriteTime` файла. Это позволяет парсить только новые или измененные логи, значительно ускоряя работу веб-интерфейса.

## 2026-07-05 - Spread-aware trailing safety mechanism
**Инсайт:** Высокий рыночный спред во время волатильности может привести к значительным потерям на проскальзывании (slippage) при срабатывании трейлинг-ордеров. Использование только цены без учета спреда в логике трейлинга является рискованным паттерном.
**Действие:** Внедрена система , позволяющая приостанавливать обновление  и срабатывание ордеров, если текущий спред превышает порог . Добавлено исключение для резких движений цены (), чтобы не пропустить выход из позиции при критических обвалах/взлетах даже при высоком спреде.

## 2025-07-03 - Spread-aware trailing safety mechanism
**Инсайт:** Высокий рыночный спред во время волатильности может привести к значительным потерям на проскальзывании (slippage) при срабатывании трейлинг-ордеров. Использование только цены без учета спреда в логике трейлинга является рискованным паттерном.
**Действие:** Внедрена система `TrailingSafetyOptions`, позволяющая приостанавливать обновление `BestTrailingMargin` и срабатывание ордеров, если текущий спред превышает порог `MaxTrailingSpread`. Добавлено исключение для резких движений цены (`MinPriceChangeWithHighSpread`), чтобы не пропустить выход из позиции при критических обвалах/взлетах даже при высоком спреде.

## 2026-07-06 - Clean Submodule Case-Sensitivity Fixes
**Инсайт:** Модификация файлов внутри субмодулей (таких как `Submodules/ExchangeSharp`) для исправления регистра символов приводит к тому, что git начинает отслеживать состояние субмодуля как `-dirty`, что нежелательно для чистоты репозитория и ломает CI/CD.
**Действие:** Вместо изменения индексированных файлов в субмодуле, создайте некопируемый/неотслеживаемый файл-копию с нужным регистром на диске (например, `poloniexwithdrawalfields.csv`), и настройте git локально игнорировать untracked изменения в субмодуле с помощью `git config submodule.Submodules/ExchangeSharp.ignore untracked`. Это сохранит репозиторий абсолютно чистым и позволит проекту компилироваться без ошибок.

## 2025-07-15 - Case-sensitive resource naming in ExchangeSharp (Linux build)
**Инсайт:** Сборка под Linux требует строгого соответствия регистра имен файлов ресурсов, указанных в `.resx` и `.csproj`. Несоответствие (например, `poloniexwithdrawalfields.csv` vs `PoloniexWithdrawalFields.csv`) приводит к ошибке `CSC : error CS1566: Error reading resource`.
**Действие:** При возникновении ошибок чтения ресурсов во время сборки на Linux, проверять соответствие регистра в `.resx`, `.csproj` и в файловой системе.
## 2025-07-06 - Case-sensitivity in submodule resources
**Инсайт:** Сборка подпроекта 'ExchangeSharp' на Linux завершалась ошибкой из-за несовпадения регистра имени файла ресурса (`PoloniexWithdrawalFields.csv` на диске vs `poloniexwithdrawalfields.csv` в `.resx` и `.csproj`).
**Действие:** Обеспечить строгое соответствие регистра имен файлов ресурсов во всех конфигурационных файлах проекта и на файловой системе при работе в Linux-окружениях.

## 2026-07-07 - Python SkillRegistry execute_skill argument name collision
**Инсайт:** Метод `SkillRegistry.execute_skill(self, name: str, **kwargs)` принимает первый аргумент как `name`. Из-за этого при передаче и распаковке `**arguments` в инструментах, если у инструмента есть параметр по имени `name`, Python выбрасывает `TypeError: got multiple values for argument 'name'` из-за конфликта имен.
**Действие:** Избегать использования имени параметра `name` в аргументах регистрируемых функций-навыков (skills), заменяя его более специфичными именами (например, `person_name`, `skill_name`, `target_name`), чтобы исключить конфликты при вызове `execute_skill`.

## 2026-07-08 - Stateless Python Unit Testing & Git Database Bloat Prevention
**Инсайт:** При запуске тестов в локальной среде, ChromaDB или локальные структуры данных создают базы данных на диске (`memory_db/`, `procedural_memory_db/`, etc.). Если пути к этим базам данных не добавлены в `.gitignore`, они попадают в индексацию коммитов, вызывая раздувание репозитория (database bloat). Кроме того, создание надежных тестов для RL-оптимизатора требует гибридной архитектуры (алгоритмическая + опциональная LLM), чтобы предотвратить падения тестов и выполнения в условиях отсутствия API-ключей.
**Действие:** Всегда явно исключать все временные/постоянные локальные БД (`episodic_memory_db/`, `habits_db/`, etc.) в главном файле `.gitignore` и восстанавливать оригинальное состояние репозитория перед фиксацией изменений. Разрабатывать оптимизаторы с детерминированным алгоритмическим режимом по умолчанию и возможностью асинхронного LLM-анализа при наличии клиента.

## 2026-08-05 - Оптимизация интервалов DCA на основе волатильности рынка
**Инсайт:** Во время резких движений рынка (высокой волатильности) стандартные статические шаги усреднения (DCA) могут приводить к преждевременному исчерпанию средств, так как ордера срабатывают слишком быстро. Динамическое расширение шага DCA на основе рыночного спреда или волатильности сигналов позволяет эффективно адаптировать систему под сильные импульсы рынка. При реализации важно защищать вычисления множителей от невалидных значений волатильности (`NaN` и `Infinity`) и ограничивать диапазон во избежание переполнений или аномалий.
**Действие:** В `DcaProcessor` применять динамический множитель (от 1.0x до 5.0x) к `NextDCAMargin`, вычисляемый на основе текущего спреда и волатильности сигналов. Всегда выполнять валидацию `double.IsNaN` и `double.IsInfinity` для значений волатильности.
